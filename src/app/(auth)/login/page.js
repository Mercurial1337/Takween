import LoginClient from './LoginClient';

export const metadata = {
  title: 'Sign In | Takween',
  description: 'Log in to your Takween account to find team partners, join teams, and collaborate.',
};

export default function LoginPage() {
  return <LoginClient />;
}
