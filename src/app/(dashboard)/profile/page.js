import ProfileEditClient from './ProfileEditClient';

export const metadata = {
  title: 'Edit Profile',
  description: 'Manage your contact details, academic level, professional links, and select your skill sets on Takween.',
};

export default function ProfilePage() {
  return <ProfileEditClient />;
}
